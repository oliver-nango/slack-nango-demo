import { ParserError } from './errors.js';
import type { NangoModel, NangoModelField, NangoYamlModel, NangoYamlModelFields } from '@nangohq/types';
export declare class ModelsParser {
    parsed: Map<string, NangoModel>;
    references: Set<string>;
    errors: ParserError[];
    warnings: ParserError[];
    raw: NangoYamlModel;
    constructor({ raw }: {
        raw: NangoYamlModel;
    });
    parseAll(): void;
    get(name: string): NangoModel | undefined;
    parseOne({ name, fields, stack }: {
        name: string;
        fields: NangoYamlModelFields;
        stack: Set<string>;
    }): void;
    ifModelParse({ name, stack }: {
        name: string;
        stack: Set<string>;
    }): true | false;
    parseFields({ fields, stack }: {
        fields: NangoYamlModelFields;
        stack: Set<string>;
    }): NangoModelField[];
}
/**
 * Get recursively (and safely) all the model names inside a model
 */
export declare function getRecursiveModelNames(parser: ModelsParser, name: string): Set<string>;
