import type { Result } from '@nangohq/types';
export declare function checkAndSyncPackageJson({ fullPath, debug }: {
    fullPath: string;
    debug: boolean;
}): Promise<Result<{
    updated: boolean;
}>>;
