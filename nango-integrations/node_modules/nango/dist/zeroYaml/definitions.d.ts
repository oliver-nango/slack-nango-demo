import type { CreateActionResponse, CreateSyncResponse } from '@nangohq/runner-sdk';
import type { ZodMetadata, ZodModel } from '@nangohq/runner-sdk/lib/types.js';
import type { NangoModel, NangoYamlParsed, ParsedNangoAction, ParsedNangoSync, Result } from '@nangohq/types';
import type * as z from 'zod';
export declare function buildDefinitions({ fullPath, debug }: {
    fullPath: string;
    debug: boolean;
}): Promise<Result<NangoYamlParsed>>;
export declare function buildSync({ filePath, params, integrationIdClean, basename, basenameClean }: {
    filePath: string;
    params: CreateSyncResponse<Record<string, ZodModel>, ZodMetadata>;
    integrationIdClean: string;
    basename: string;
    basenameClean: string;
}): Result<{
    sync: ParsedNangoSync;
    models: Map<string, NangoModel>;
}>;
export declare function buildAction({ params, integrationIdClean, basename, basenameClean }: {
    params: CreateActionResponse<z.ZodTypeAny, z.ZodTypeAny, z.ZodObject>;
    integrationIdClean: string;
    basename: string;
    basenameClean: string;
}): {
    action: ParsedNangoAction;
    models: Map<string, NangoModel>;
};
