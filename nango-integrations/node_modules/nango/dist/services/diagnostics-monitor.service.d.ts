interface MemoryStats {
    avgBytes: number;
    peakBytes: number;
}
interface CPUStats {
    avgPercent: number;
    peakPercent: number;
}
export interface DiagnosticsStats {
    durationMs: number;
    memory: {
        rss: MemoryStats;
        heapUsed: MemoryStats;
        heapTotal: MemoryStats;
        external: MemoryStats;
    };
    cpu: CPUStats;
}
export declare class DiagnosticsMonitor {
    private static readonly MAX_SAMPLES;
    private samples;
    private startTimeMs;
    private startCpu;
    private intervalId;
    private currentIntervalMs;
    private lastSample;
    private stableCount;
    start(): void;
    stop(): DiagnosticsStats;
    private takeSample;
    private detectSignificantChange;
    private scheduleNextSample;
    private calculateMemoryStats;
    private calculateCPUStats;
}
export declare function formatDiagnostics(stats: DiagnosticsStats): string;
export {};
