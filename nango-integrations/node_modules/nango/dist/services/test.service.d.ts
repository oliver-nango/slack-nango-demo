export interface IntegrationDefinition {
    syncs: Record<string, {
        output: string | string[];
    }>;
    actions: Record<string, {
        output: string | null;
    }>;
}
export interface ValidateFiltersResult {
    valid: boolean;
    error?: string;
    filteredIntegrations: Record<string, IntegrationDefinition>;
}
/**
 * Validates and filters integrations based on provided filter criteria.
 * This is a pure function that can be easily unit tested.
 */
export declare function validateAndFilterIntegrations({ integrations, integrationId, syncName, actionName }: {
    integrations: Record<string, IntegrationDefinition>;
    integrationId?: string | undefined;
    syncName?: string | undefined;
    actionName?: string | undefined;
}): ValidateFiltersResult;
/**
 * Determines if a sync should be processed for test generation.
 * When actionName is specified, syncs should be skipped (user only wants action tests).
 * When syncName is specified, only the matching sync should be processed.
 */
export declare function shouldProcessSync({ currentSyncName, syncName, actionName }: {
    currentSyncName: string;
    syncName: string | undefined;
    actionName: string | undefined;
}): boolean;
/**
 * Determines if an action should be processed for test generation.
 * When syncName is specified, actions should be skipped (user only wants sync tests).
 * When actionName is specified, only the matching action should be processed.
 */
export declare function shouldProcessAction({ currentActionName, syncName, actionName }: {
    currentActionName: string;
    syncName: string | undefined;
    actionName: string | undefined;
}): boolean;
export declare function generateSyncTest({ integration, syncName, modelName, writePath, debug }: {
    integration: string;
    syncName: string;
    modelName: string | string[];
    writePath: string;
    debug: boolean;
}): Promise<string>;
export declare function generateActionTest({ integration, actionName, output, writePath, debug }: {
    integration: string;
    actionName: string;
    output: string | null;
    writePath: string;
    debug: boolean;
}): Promise<string>;
export declare function generateTests({ absolutePath, integrationId, syncName, actionName, debug, autoConfirm }: {
    absolutePath: string;
    integrationId?: string;
    syncName?: string;
    actionName?: string;
    debug?: boolean;
    autoConfirm?: boolean;
}): Promise<{
    success: boolean;
    generatedFiles: string[];
}>;
