declare class VerificationService {
    necessaryFilesExist({ fullPath, autoConfirm, debug, checkDist }: {
        fullPath: string;
        autoConfirm: boolean;
        debug?: boolean;
        checkDist?: boolean;
    }): Promise<void>;
    filesMatchConfig({ fullPath }: {
        fullPath: string;
    }): boolean;
    preCheck({ fullPath, debug }: {
        fullPath: string;
        debug: boolean;
    }): Promise<{
        isNango: boolean;
        hasNangoYaml: boolean;
        folderName: string;
        hasIndexTs: boolean;
        isZeroYaml: boolean;
    }>;
    ensureNangoYaml({ fullPath, debug }: {
        fullPath: string;
        debug: boolean;
    }): Promise<boolean>;
}
declare const verificationService: VerificationService;
export default verificationService;
